# 产品需求文档 (PRD)：智能股价监控系统

**版本：** V1.0

**状态：** 初稿

**日期：** 2025-12-23

---

## 1. 项目背景与目标

### 1.1 背景

投资者在管理投资组合时，往往无法全天候关注股市波动。错失卖出时机或错过低吸机会是痛点。

### 1.2 核心目标

* **实时性：** 提供秒级的股价更新与监控。
* **自动化：** 用户设置阈值，系统自动触发告警。
* **多端联动：** 支持邮件、APP、网页端等多渠道通知。

---

## 2. 用户角色

1. **普通投资者：** 关注少量核心持仓，需求简单的价格提醒。
2. **职业交易员：** 关注大量标的，需求复杂的组合策略监控（如涨幅、成交量异动）。

---

## 3. 功能需求

### 3.1 核心功能模块

| 功能模块 | 功能点 | 需求描述 | 优先级 |
| --- | --- | --- | --- |
| **自选股管理** | 添加/删除股票 | 支持通过代码、拼音、全称搜索并加入监控列表。 | P0 |
| **监控规则设置** | 价格触发 | 设置“股价高于 X”或“股价低于 Y”时告警。 | P0 |
|  | 涨跌幅触发 | 设置“日内涨幅超过 Z%”或“日内跌幅超过 Z%”时告警。 | P1 |
|  | 异动触发 | 5分钟内快速上涨/下跌超过特定比例时告警。 | P2 |
| **告警通知** | 通知渠道 | 支持系统弹窗、邮件通知、Webhook（如钉钉/飞书）。 | P0 |
|  | 通知频率 | 可设置“仅触发一次”或“持续间隔触发”。 | P1 |
| **实时看板** | 数据可视化 | 提供监控列表的实时价格柱状图或简易K线图。 | P2 |

---

## 4. 业务流程

1. **添加标的：** 用户在搜索框输入股票代码（如：AAPL）。
2. **配置策略：** 用户选择监控指标（价格、涨跌幅），设定阈值（例如：价格 > 200）。
3. **系统轮询/推送：** 后端服务实时对接交易所数据源，对比当前价与用户设置的阈值。
4. **触发告警：** 当条件满足，系统立即调用通知接口。

---

## 5. 系统架构建议

为了保证监控的准确性和低延迟，建议采用以下技术栈逻辑：

* **数据接入层：** 对接第三方行情API（如富途、长桥或聚宽）。
* **规则引擎：** 使用计算引擎（如 Redis 缓存当前价）进行快速匹配。
* **推送服务：** 异步处理通知，避免阻塞监控主进程。

---

## 6. 非功能需求

* **性能：** 单个规则的计算延迟应在 **500ms** 以内。
* **并发：** 系统应支持至少 **10,000** 个监控规则同时运行。
* **准确性：** 价格数据需与交易所实盘数据保持一致，误差率低于 **0.01%**。
* **安全性：** 用户配置的监控策略属于个人隐私，需加密存储。

---

## 7. 后续规划 (V2.0)

* **智能预测：** 基于历史数据提供支撑位/压力位自动监控。
* **组合监控：** 对整个持仓组合的净值波动进行监控。
* **模拟交易对接：** 触发告警后，提供一键跳转下单功能。

---

**你想让我为你详细补充哪一部分？例如：细化告警通知的具体 API 设计，或者是设计自选股页面的 UI 原型描述？**
